export enum Gender {
    MALE = 'Male',
    FEMALE = 'Female',
    NON_BINARY = 'Non-binary',
}

export enum PlanDuration {
    MONTHLY = 'Monthly',
    QUARTERLY = 'Quarterly',
    YEARLY = 'Yearly',
}

export enum PlanType {
    FREE = 'Free',
    PAID = 'Paid',
}

export enum PaymentStatus {
    PENDING = 'pending',
    SUCCESS = 'success',
    FAILED = 'failed',
    REFUND = 'refund',
    CANCELED = 'canceled',
}
